say "--- Aether AI v2.5 Training Environment ---"

// 1. Array support for gradients/weights
say "Initializing weights tensor..."
array weights from 5

// 2. Floating point support
set learning_rate to 0.01
say "Learning Rate:"
say learning_rate

// 3. Smart Debugging Test
// I will intentionally leave out a 'to' or 'times' in a separate script, 
// but here let's show a perfect run.

set epoch to 1
while epoch < 4 {
    say "Epoch:"
    say epoch
    
    set loss to random(1, 100)
    say "Current Loss:"
    say loss
    
    set epoch to epoch + 1
}

say "AI Model training complete."
