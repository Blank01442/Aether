// Dialogs library for Aether
// Windows interface functions

task show_message {
    alert("Hello from Aether!")
}

task get_screen_info {
    set w to screen_width()
    set h to screen_height()
    say "Screen resolution:"
    say w
    say "x"
    say h
}